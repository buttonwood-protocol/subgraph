{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network mainnet",
    "build-kovan": "graph build --network kovan",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ buttonwood/subgraph",
    "deploy-kovan": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ fiddlekins/tranche-kovan",
    "create-local": "graph create --node http://localhost:8020/ buttonwood/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ buttonwood/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 buttonwood/subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.32.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.28.4"
  }
}
